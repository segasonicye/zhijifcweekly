# Netlify 自动增量更新指南

## 方案对比

### 方案1: Netlify CLI 手动触发 (推荐 - 最简单)
每次更新内容后运行一条命令即可

### 方案2: Git + Netlify 自动部署 (推荐 - 最自动化)
推送代码到GitHub,Netlify自动部署

### 方案3: Netlify Hook (高级)
通过Webhook触发部署

---

## 方案1: Netlify CLI 手动部署 (当前使用)

### 使用方法
```bash
# 每次更新内容后运行
npm run deploy
```

### 工作原理
1. 脚本自动生成所有HTML文件
2. Netlify CLI检测变化
3. 只上传修改过的文件 (增量更新)
4. 速度快,流量消耗少

### 优点
- ✅ 简单直接,一条命令搞定
- ✅ Netlify自动检测变化,只上传修改文件
- ✅ 不需要Git仓库
- ✅ 免费额度完全够用

### 缺点
- ❌ 需要手动运行命令

---

## 方案2: Git + Netlify 自动部署 (最自动化)

### 步骤1: 初始化Git仓库

```bash
# 初始化Git
git init

# 创建.gitignore
cat > .gitignore << 'EOF'
node_modules/
.DS_Store
*.log
.vscode/
.idea/
EOF

# 首次提交
git add .
git commit -m "Initial commit: 足球队战报系统"
```

### 步骤2: 推送到GitHub

```bash
# 在GitHub创建新仓库后
git remote add origin https://github.com/你的用户名/zhiji-football.git
git branch -M main
git push -u origin main
```

### 步骤3: 在Netlify连接GitHub

1. 访问 https://app.netlify.com
2. 点击 "Add new site" → "Import an existing project"
3. 选择 "GitHub"
4. 授权Netlify访问你的GitHub
5. 选择 `zhiji-football` 仓库
6. 配置构建设置:
   - **Build command**: 留空 (静态站点不需要构建)
   - **Publish directory**: `output`
   - 点击 "Deploy site"

### 步骤4: 日常更新流程

```bash
# 1. 编辑内容后
git add .
git commit -m "添加新战报"
git push

# Netlify自动检测到push,自动部署!
```

### 优点
- ✅ 完全自动化,推送即部署
- ✅ Netlify只上传变化的文件
- ✅ 有完整的版本历史
- ✅ 多人协作方便
- ✅ 可以回滚到任意版本

### 缺点
- ❌ 需要使用Git
- ❌ 需要GitHub账号

---

## 方案3: 优化当前deploy脚本

我已经为你创建了一个优化的部署脚本,它会:
- 自动生成所有HTML
- 调用Netlify CLI部署
- Netlify自动检测变化,只上传修改的文件

### 当前工作流程

```bash
# 添加新战报
npm run new

# 添加照片
npm run add-photos

# 一键部署 (Netlify自动增量更新)
npm run deploy
```

---

## Netlify 增量更新原理

Netlify CLI (`netlify deploy`) 默认就是增量更新:

### 1. 文件指纹识别
- Netlify会计算每个文件的哈希值
- 只上传哈希值变化的文件

### 2. 智能缓存
- 未变化的文件直接使用CDN缓存
- 浏览器缓存未过期的资源

### 3. 并行上传
- 多个文件并行上传
- 大幅提升部署速度

### 实际效果
- 全新部署: 可能需要1-2分钟
- 增量更新: 通常只需10-20秒
- 只修改文字: 可能只要5秒!

---

## 部署优化建议

### 1. 预览部署
Netlify支持每个Git push生成预览URL:
- 主分支: 正式网站
- 其他分支: 预览URL
- 可以先预览再发布

### 2. 部署通知
添加部署通知,了解部署状态:
```javascript
// 可以在deploy.js中添加通知功能
```

### 3. 环境隔离
- **Production**: 正式网站
- **Development**: 测试环境
- 不同环境使用不同配置

---

## 推荐方案

### 对于你当前的使用场景

**推荐: 方案1 (Netlify CLI)**
- 你已经有完整的deploy脚本
- 操作简单,一条命令完成
- Netlify自动增量更新
- 完全满足每周更新的需求

**进阶可选: 方案2 (Git + 自动部署)**
- 如果你熟悉Git
- 如果需要版本控制
- 如果需要多人协作

### 选择建议
- **个人使用**: 方案1就足够了
- **团队协作**: 建议方案2
- **高频更新**: 方案2更省事

---

## 日常维护建议

### 定期备份
```bash
# 定期将项目推送到Git备份
git add .
git commit -m "Backup"
git push
```

### 监控部署状态
访问 Netlify Dashboard 查看部署历史和状态

### 域名配置
在Netlify设置中添加自定义域名,如:
- `zhiji.yourdomain.com`
- `www.zhiji-football.com`

---

## 快速参考

### 手动部署 (当前使用)
```bash
npm run deploy
```

### Git自动部署 (推荐升级)
```bash
git add .
git commit -m "更新"
git push
```

### 查看部署日志
访问 Netlify → Sites → 你的站点 → Deploys

---

## 总结

Netlify的增量更新是**自动的**!
- ✅ 你不需要手动配置
- ✅ Netlify CLI默认就是增量上传
- ✅ 只会上传变化的文件
- ✅ 速度快,流量省

你的 `npm run deploy` 已经在使用增量更新了! 🎉
