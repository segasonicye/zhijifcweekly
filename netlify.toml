[build]
  publish = "output"
  command = "npm run matches"

[build.environment]
  NODE_VERSION = "18"

# 说明：
# 要节省Netlify构建额度，请在Netlify控制台关闭自动部署：
# Site settings → Build & deploy → Continuous deployment → 停用自动部署
# 需要部署时，使用命令：netlify deploy --prod
